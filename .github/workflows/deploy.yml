# Deployment is now handled by Vercel (brad-zqh-github-io.vercel.app)
# This workflow is disabled.
name: Disabled - Deployment moved to Vercel

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deployment is handled by Vercel"
